[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
black = "==19.10b0"  # this version is pinned since the library does not use semver
pylint = "~=2.4.4"
pytest = "~=5.4.1"
mypy = "==0.770"  # this version is pinned since the library does not use semver

[packages]
asana = "~=0.10.0"
python-dotenv = "~=0.13.0"

[requires]
python_version = "3.8"
